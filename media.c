# include <stdio.h>

int main(){
    char nome[50];
    float notal, nota2, nota3, media;
    printf("%s", nome);

    printf("Digte o nome do aluno: ");
    scanf("%s", nome);

    printf("Digite as 3 notas: ");
    scanf("%f %f %f",&nota1, &nota2, &nota3);

    media = (nota01 )
}